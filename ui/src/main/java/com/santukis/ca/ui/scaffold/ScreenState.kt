package com.santukis.ca.ui.scaffold

import androidx.compose.runtime.Stable

/**
 * A marker interface for all screen state classes.
 * Screen state represents the data that is needed to render a screen.
 * It should be a data class or an immutable object.
 */
@Stable
interface ScreenState
